name: chomi
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # ✅ Firebase core features (essential)
  firebase_core: ^3.15.1
  firebase_auth: ^5.6.2
  cloud_firestore: ^5.6.11
  firebase_storage: ^12.4.9
  video_trimmer: 5.0.0
  visibility_detector: ^0.4.0
  smooth_page_indicator: ^1.2.1
  flutter_image_compress: ^2.1.0
  cached_network_image: ^3.3.1
  photo_view: ^0.15.0
  file_picker: ^10.2.0
  just_audio: ^0.10.4
  audioplayers: ^6.5.0
  #ffmpeg_kit_flutter_https: 6.0.3-LTS
  path_provider: ^2.1.5
  camera: ^0.11.2
  record: ^6.0.0
  #google_mobile_ads: ^3.0.0

  # ✅ For images and videos
  image_picker: ^1.1.2
  video_player: ^2.10.0

  # ✅ For sharing, preferences, date formatting, permissions
  share_plus: ^11.0.0
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  permission_handler: ^12.0.1

  # ✅ For state management
  provider: ^6.1.5

  # ❌ Temporarily removed/commented out for build safety
  # csv: ^6.0.0
  #path_provider: ^2.1.5
  #geolocator: ^14.0.2
  #geocoding: ^4.0.0
  flutter_sound: ^9.28.0
  audio_waveforms: ^1.3.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4   # ✅ Added for app icon generation

flutter:
  uses-material-design: true

  assets:
    - assets/avatar.png

flutter_icons:
  android: true
  ios: true
  image_path: "assets/avatar.png"


